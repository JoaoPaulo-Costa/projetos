// Combinando Arrays

const numero = [1,2,3,4,5,6];

const combinado = numero.join(',');
console.log(combinado);

const frase = 'Olá, meu caro';

const resultado = frase.split(' '); // Separa baseado no parâmetro
console.log(resultado);

console.log(resultado.join('-'));